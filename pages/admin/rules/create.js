import React from 'react';
import Createrule from '../../../components/admin/createRule'
import Layout from '../../../components/Layout'

const createRules = () => {
    return (
        <Layout>
            <Createrule />
        </Layout>
    );
}

export default createRules;