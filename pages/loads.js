import React from 'react';
import Load from '../components/Loads/loads'
import Layout from '../components/Layout'

const loads = () => {
    return (
        <Layout>
            <Load />
        </Layout>
    );
}

export default loads;